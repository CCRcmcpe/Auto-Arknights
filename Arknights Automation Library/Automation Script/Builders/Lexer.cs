using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;

namespace REVUnit.AutoArknights.Library.Automation_Script
{
    /// <summary>
    /// 处理的第2层
    /// </summary>
    public class Lexer
    {
        /*private class Screen
        {
            private readonly string code;
            public int Position { get; set; } = -1;
            private readonly Regex tokenRegex = new Regex(@"");

            public bool End => code.Length == index;

            public Screen(string code)
            {
                this.code = code;
                var regex = new Regex();
            }

            public char Peek()
            {
                return code[index + 1];
            }

            public char Next()
            {
                return code[++index];
            }

            public char NextString()
            {
                var indexOfSpace = code.IndexOf(' ', Position);
                
            }
        }*/


    }
}